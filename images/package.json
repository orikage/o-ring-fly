{
  "name": "o-ring-images",
  "version": "1.0.0",
  "description": "images.o-ring-fly — Photo & Illustration Archive",
  "scripts": {
    "dev": "bun run src/index.tsx",
    "start": "bun run src/index.tsx",
    "build:css": "npx tailwindcss -i src/style.css -o public/style.css",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "marked": "^14.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0",
    "@types/bun": "latest"
  }
}
