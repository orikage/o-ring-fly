{
  "name": "o-ring-images",
  "version": "1.0.0",
  "description": "o-ring-fly images subdomain — Arknights tactical photo gallery",
  "scripts": {
    "dev": "bun run src/index.tsx",
    "start": "bun run src/index.tsx",
    "test": "bun test",
    "build:css": "npx tailwindcss -i src/style.css -o public/style.css"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "marked": "^14.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0",
    "@types/bun": "latest"
  }
}
